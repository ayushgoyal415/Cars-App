/**@param {string} s */
const $ = s => document.querySelector(s);

const LS_port = 5500;
const LS_domain = `http://localhost:${LS_port}/JS/html`;

const HTTPS_PORT = 3001;
const HTTPS_domain = `https://localhost:${HTTPS_PORT}`;
